---
- hosts: localhost
  become: yes
  tasks:
  - name: Copy index.html to /var/www/html
    shell: cp /data/system_facts_report.html /var/www/html/index.html

  - name: Restart httpd
    service:
      name: httpd
      state: restarted
